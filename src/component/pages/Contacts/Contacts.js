import React from 'react';
import './styles.scss'

const Contacts = () => {
    return (
        <div className="contacts">
            Day la Contacts
        </div>
    );
};

export default Contacts;
